package automaton

import (
	// . "github.com/balzaczyy/golucene/test_framework"
	"testing"
)

func TestUTF16InUTF8Order(t *testing.T) {
	// numStrings := AtLeat(1000)
	// utf8 := make([][]byte, numStrings)
	// utf16 := make([][]rune, numStrings)

	// for i := 0; i < numStrings; i ++ {
	// 	s := RandomUnicodeString(Random())
	// 	utf8[i] := []byte(s)
	// 	utf16[i] := []rune(s)
	// }
}
